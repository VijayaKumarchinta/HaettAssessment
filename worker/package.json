{
  "name": "haett-assessment-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply haett-db --remote",
    "db:init": "wrangler d1 migrations apply haett-db --local"
  }
}
